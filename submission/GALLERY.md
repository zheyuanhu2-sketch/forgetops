# Devpost screenshot gallery

Upload these truthful 1920×1080 product captures in this order. They contain only the synthetic
case `DSR-2026-0042` and no raw subject identifier, credential, or tenant data.

## 1. DataHub context becomes an exact approval scope

File: `demo-video/capture/product-states/01-approve.png`

Caption:

> DataHub schema, ownership, governance signals, and lineage reconstruct seven affected assets.
> ForgetOps keeps one legal hold and one manual-review outcome visible, then binds approval to
> exactly five permitted mutations.

## 2. One transaction produces inspectable receipts

File: `demo-video/capture/product-states/03-receipts.png`

Caption:

> The approved scope executes idempotently in one transaction while ForgetOps records action-level
> receipts. Protected exceptions remain outside the transaction and rollback stays armed until
> every postcondition passes.

## 3. Write, then verify from a fresh session

File: `demo-video/capture/product-states/06-evidence-contract.png`

Caption:

> A separate approval writes tags, structured properties, and one reusable evidence document back
> to DataHub. A fresh mutation-disabled MCP session reads all seven entities and the document back
> before ForgetOps reports `PARTIAL_VERIFIED`.
