# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

## ForgetOps visual decision

- The accepted visual direction is Product Design option 3, "Case Filmstrip / Evidence Timeline".
- Source visual truth: `%CODEX_HOME%\generated_images\019f6572-f190-7233-81e0-3310e1626fe2\exec-0f5c741b-2dd6-4fdf-9da2-7b7473acc8aa.png`.
- Preserve the dark graphite editorial canvas, six-stage filmstrip, evidence timeline, right-side impact map, explicit approval boundary, and evidence footer.
- Use the repository's real ForgetOps sample facts. The generated concept's invented asset labels or timestamps are not authoritative.
- The execution approval and DataHub write-back approval must remain visibly distinct.
- The core workflow must be interactive from approval through execution, verification, and write-back read-back.
